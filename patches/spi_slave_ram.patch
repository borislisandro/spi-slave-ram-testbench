diff --git a/Codes/RTL/RAM.v b/Codes/RTL/RAM.v
index 061c0ee..67f9795 100644
--- a/Codes/RTL/RAM.v
+++ b/Codes/RTL/RAM.v
@@ -1,3 +1,5 @@
+`timescale 1ns/1ps
+
 module RAM #(
     parameter MEM_DEPTH = 256,
     parameter ADDR_SIZE = 8
@@ -36,4 +38,4 @@ module RAM #(
             end
         end 
     end
-endmodule
\ No newline at end of file
+endmodule
diff --git a/Codes/RTL/SPI.v b/Codes/RTL/SPI.v
index 26f0096..4b4893d 100644
--- a/Codes/RTL/SPI.v
+++ b/Codes/RTL/SPI.v
@@ -1,3 +1,5 @@
+`timescale 1ns/1ps
+
 module SPI (
     input clk, rst_n,
     input MOSI, SS_n,
@@ -69,19 +71,19 @@ module SPI (
             rx_data <= 0;
             MISO <= 0;
             rx_valid <= 0;
-            read_trans = 0;
+            read_trans <= 0;
         end
         else if (cs != IDLE && cs != CHK_CMD) begin
                 if(cs == READ_ADD)
-                    read_trans = 1;
+                    read_trans <= 1;
                 else if(cs == READ_DATA)
-                    read_trans = 0;
+                    read_trans <= 0;
 
                 if (counter<=9)
-                    rx_data = {MOSI, rx_data[9:1]};
+                    rx_data <= {MOSI, rx_data[9:1]};
                 
                 if (counter == 9)
-                    rx_valid = 1;
+                    rx_valid <= 1;
                 else if (counter == 11 || counter == 0)
                     rx_valid <= 0;
                     
@@ -97,6 +99,6 @@ module SPI (
                 end
 
                 counter <= counter + 1;
-        end else counter = 0;
+        end else counter <= 0;
     end
-endmodule
\ No newline at end of file
+endmodule
diff --git a/Codes/RTL/instantiation.v b/Codes/RTL/instantiation.v
index fe1e88d..ee589b1 100644
--- a/Codes/RTL/instantiation.v
+++ b/Codes/RTL/instantiation.v
@@ -1,10 +1,11 @@
+`timescale 1ns/1ps
+
 module instantiation (
     input clk, rst_n,
     input MOSI, SS_n,
 
     output MISO
 );
-
     wire tx_valid, rx_valid;
     wire [9:0]rx_data;
     wire [7:0]tx_data;
@@ -30,4 +31,4 @@ module instantiation (
         .dout(tx_data),
         .tx_valid(tx_valid)
     );
-endmodule
\ No newline at end of file
+endmodule
